<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.itsorderkds">

    <!-- Sieć i komunikacja -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Powiadomienia -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Usługi pierwszoplanowe -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FOREGROUND_SERVICE_DATA_SYNC: SocketService utrzymuje połączenie WebSocket (socket.io)
         do serwera zamówień restauracyjnych w celu odbierania zamówień KDS w czasie rzeczywistym -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Budzenie urządzenia przy nowych zamówieniach -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Drukarka Bluetooth (opcjonalna) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:required="false"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"    android:required="false"/>

    <!-- Drukarka USB (opcjonalna) -->
    <uses-feature android:name="android.hardware.usb.host" />

    <!-- Firebase Cloud Messaging -->
    <uses-permission android:name="com.itsorderkds.C2D_MESSAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <permission android:name="com.itsorderkds.permission.C2D_MESSAGE" android:protectionLevel="signature" />
    <application
        android:name=".ItsChat"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ITSChat">

        <!-- Wsparcie dla 16 KB stron pamięci (wymagane przez Google Play dla targetSdk 35+) -->
        <property
            android:name="android.app.property.16KB_PAGE_SIZE_ENABLED"
            android:value="true" />

        <activity
            android:name=".ui.theme.home.HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home" />
<!--        <activity-->
<!--            android:name=".ui.product.ProductActivity"-->
<!--            android:exported="false"-->
<!--            android:label="@string/title_product_activity" />-->
<!--        <activity-->
<!--            android:name=".ui.settings.log.LogsActivity"-->
<!--            android:exported="false"-->
<!--            android:label="@string/Logs" />-->
        <!-- Ustawienie motywu aplikacji -->
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ITSChat.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity-->
<!--            android:name=".ui.settings.SettingsActivity"-->
<!--            android:exported="false"-->
<!--            android:label="@string/Settings"-->
<!--            android:parentActivityName=".ui.theme.home.HomeActivity"/>-->
        <activity
            android:name=".ui.settings.printer.PrinterSettingsActivity"
            android:exported="false" />


        <service
            android:name=".service.SocketService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter android:priority="100">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
<!--        <service android:name=".service.OrderAlarmService" android:exported="false"-->
<!--            android:foregroundServiceType="mediaPlayback"-->
<!--            />-->

        <receiver
            android:name="com.google.firebase.iid.FirebaseInstanceIdReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="com.itsorderkds" />
            </intent-filter>
        </receiver>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBnyBPctD0w_G9Qo6oIaWFn1rkGPtOcKec" />

    </application>

</manifest>
